NEXT_PUBLIC_PRIVY_APP_ID=

PRIVY_APP_SECRET=

ALCHEMY_API_KEY=

ALCHEMY_KEY_PRICES=
ALCHEMY_KEY_PORTFOLIO=
ALCHEMY_KEY_FALLBACK=

DEXTOPUS_API_KEY=

# LI.FI aggregator for EVM swaps (Base, Arbitrum, Polygon). Key from
# portal.li.fi. Kept server-side; requests proxy through /api/lifi.
LIFI_API_KEY=
# Optional swap fee in basis points (e.g. 25 = 0.25%). Requires fee-receiver
# wallets configured in the LI.FI portal. Unset or 0 means no WSWS fee.
LIFI_FEE_BPS=

# Polymarket prediction markets. Builder credentials from polymarket.com
# (Settings > Builders). SECRET and PASSPHRASE are server-only.
POLYMARKET_BUILDER_API_KEY=
POLYMARKET_BUILDER_SECRET=
POLYMARKET_BUILDER_PASSPHRASE=
# Builder code for on-chain trade attribution (public, embedded in signed orders).
NEXT_PUBLIC_POLYMARKET_BUILDER_CODE=

# King of Night vault game (world-street-vault service). Called directly from
# the client, not proxied — the gateway handles CORS and no key is required.
# API is https://<api-host>/v1/world-street-vault (path fixed). WS is the
# realtime hub root (wss://<ws-host>/); the client subscribes to the topic
# vault:king-of-night. Both hostnames are gateway-config; confirm with the
# platform team. The game falls back to REST polling when the socket is down.
NEXT_PUBLIC_VAULT_API_URL=
NEXT_PUBLIC_VAULT_WS_URL=
NEXT_PUBLIC_VAULT_CONTRACT_ADDRESS=
