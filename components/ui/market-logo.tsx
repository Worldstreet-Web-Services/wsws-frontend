// The mARKet lockup is drawn by @ark/chrome, the package that also draws the
// rail and the phone tab bar, so the logo on the rail and the one on the
// landing page and sign-in screens are the same component in every app.
export { ArkLogo as MarketLogo, type ArkLogoProps as MarketLogoProps } from "@ark/chrome/logo";
