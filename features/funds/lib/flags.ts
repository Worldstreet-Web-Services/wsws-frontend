// Naira bank withdrawal is turned off at the entry point while the offramp is
// unavailable. The screen and its API client are left in place, so restoring
// the flow is this one value; nothing else needs to change.
export const BANK_WITHDRAW_ENABLED = false;
